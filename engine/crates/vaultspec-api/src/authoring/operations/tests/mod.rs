//! Operations test suite — shared helpers + per-family groups (the test module alone
//! exceeded the cap). Verbatim split, contiguous ranges.
mod helpers;
mod preview_edit_rename;
mod section_and_create;
