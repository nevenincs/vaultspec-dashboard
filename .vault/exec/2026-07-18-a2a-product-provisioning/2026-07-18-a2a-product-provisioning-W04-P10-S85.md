---
tags:
  - '#exec'
  - '#a2a-product-provisioning'
date: '2026-07-21'
modified: '2026-07-21'
step_id: 'S85'
related:
  - "[[2026-07-18-a2a-product-provisioning-plan]]"
---

# Publish Scoop metadata only when its phase-zero matrix and complete real-artifact downgrade proof pass, otherwise mark Scoop unsupported and require an ADR revisit

## Scope

- `.github/workflows/release.yml`

## Description

- Authored the fail-closed Scoop publication gate: a reusable `channel-publish-gate` that reads the support matrix as the proof-of-record and refuses to publish any channel not marked `supported`, additionally asserting the phase-zero feasibility proof workflow exists; wired into scoop-bump as a `needs:` gate so the manifest bump cannot publish until both conditions hold. Sited in a Dist-safe workflow, never the autogenerated release.yml.

## Outcome

Scoop publication is gated fail-closed on the matrix status and the phase-zero proof; both conditions fail closed today. The YAML is valid and the jq logic was proven against the real matrix.

## Notes

RESIDUAL — the phase-zero clean-machine downgrade proof (S147/S149) runs at release on clean runners; the matrix flips a channel to `supported` only on a genuine pass. Left UNTICKED until that proof runs.
