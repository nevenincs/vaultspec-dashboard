---
tags:
  - '#exec'
  - '#agent-panel'
date: '2026-08-01'
modified: '2026-08-01'
body_schema: 'body-v1'
body_hash: 'sha256:dd4ead9b28f0d6acddce04627dae7a78d0737f9ebf829ce0932f1b8cd8c7465b'
step_id: 'S02'
related:
  - "[[2026-08-01-agent-panel-plan]]"
---

# Add the solo doc-editor team preset (pipeline topology, one worker, authoring_bridge on, served model profiles limited to live-proven providers)

## Scope

- `src/vaultspec_a2a/team/presets/teams/`

## Description

- Add the `vaultspec-doc-editor` team preset: pipeline topology with a single worker, `authoring_bridge = true` arming the engine MCP propose/read tools, `mcp_servers` including `vaultspec-rag` for corpus grounding.
- Declare served model profiles limited to live-proven providers: codex as the team default, one `zai` overlay profile (credential-gated on `ZAI_AUTH_TOKEN`), mirroring the adr-research zai lane pattern.
- Register the preset in the parametrized structural-compile case list.

## Outcome

Landed in commit f658938a. Structural compile test pins topology pipeline, worker-node set exactly `vaultspec-doc-editor`, no supervisor. The served-profile test iterates every declared profile dynamically and asserts each resolves to codex or zai - adding any other provider goes red. Review-confirmed: no other provider name occurs anywhere in the new TOMLs.

## Notes

The rag MCP grounding server is included in the harness alongside the bridge - an addition over the clone source, deliberate.
