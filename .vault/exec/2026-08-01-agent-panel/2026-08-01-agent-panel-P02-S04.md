---
tags:
  - '#exec'
  - '#agent-panel'
date: '2026-08-01'
modified: '2026-08-01'
body_schema: 'body-v1'
body_hash: 'sha256:3ad086c12235bdef060974964922c690a9938b2fba678a45a5412ac5dff029bd'
step_id: 'S04'
related:
  - "[[2026-08-01-agent-panel-plan]]"
---

# Extend the research_adr compiler with the Plan phase (plan-author role, doc-reviewer inner loop, phase-gate interrupt at Gate 3) and extend supported_capabilities with plan_document

## Scope

- `src/vaultspec_a2a/graph/`

## Description

- Extend the `research_adr` compiler with the Plan phase: new `plan_author`, `plan_review`, `plan_submit`, and `plan_gate` nodes; `adr_gate.approved_target` routes to `plan_author` instead of END; `plan_gate` approves to END and revises back to `plan_author`.
- Reuse the existing doc-reviewer worker node and phase-gate interrupt machinery verbatim - same interrupt/resume wire contract as the shipped gates.
- Extend `supported_capabilities()` to research, ADR, and plan documents (served through the existing presets-list projection).
- Extend the submitter grounding guard so a plan proposal must cite the applied ADR plus research/reference, mirroring the ADR-grounding guard.
- Wire the plan phase into the worker graph lifecycle with document role `vaultspec-planner`, doc type plan, and a PLAN READY completion sentinel.

## Outcome

Landed in commit 69f01764 on feature/agent-flow. Full non-service unit suite 1822 passed, 0 failed. New integration tests drive the compiled graph through all three gates over a real checkpointer: parks at Gate 3 with a plan proposal, approve completes, request-changes re-parks with a fresh proposal id. Independently review-confirmed with the would-it-go-red standard on each wiring assertion.

## Notes

The wheel-inventory contract test was retroactively extended with the P01 doc-editor TOMLs in the same commit - a latent red caught before it fired.
