---
tags:
  - '#exec'
  - '#agent-panel'
date: '2026-08-01'
modified: '2026-08-01'
body_schema: 'body-v1'
body_hash: 'sha256:63b59a77c36aa3c4e7a0a80be30bb18120a54a7344aea50370ebf18ac17c94af'
step_id: 'S41'
related:
  - "[[2026-08-01-agent-panel-plan]]"
---

# Wire the clarification node into the research_adr topology as a pre-diverge ground decision point - the researcher role either emits a structured clarify sentinel (parking the run on the questionnaire) or proceeds to fan-out, with a deterministic-profile test forcing the ask

## Scope

- `src/vaultspec_a2a/graph/`

## Description

- Add the ground node: one triage turn on the researcher role before diverge, emitting either a ready sentinel (straight to fan-out) or a clarification sentinel plus a JSON question array (park on the questionnaire, rejoin at diverge once answered - one round per run).
- Malformed or absent sentinels fail OPEN to research, mirroring the doc-review router discipline - a garbled triage can never wedge the pipeline.
- Deterministic-profile tests force each branch: the clarification proof parks on a real interrupt, resumes with an answer, and confirms the fan-out and research gate genuinely follow.

## Outcome

Landed in commit ffcc64c7 with the follow-up closer fcf94b99 (question-id grammar anchored to exclude a leading hyphen, matching the engine token rule exactly; the plan-revision call-count assertion restored). Review-confirmed across both commits; 92 touched-suite tests green.

## Notes

The closer's first test attempt exposed that bounding truncates before filtering, so a fifth list entry pushed a valid id out of consideration - caught by running the test, landed as its own case. The filter-then-truncate reorder is the accepted low-severity follow-on from the final review.
