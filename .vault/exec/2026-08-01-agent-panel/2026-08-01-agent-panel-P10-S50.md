---
tags:
  - '#exec'
  - '#agent-panel'
date: '2026-08-01'
modified: '2026-08-01'
body_schema: 'body-v1'
body_hash: 'sha256:6546fd7d6975b9e734c9fcc375b060b5275a5b7ddca5395b6d5c544fee2463ec'
step_id: 'S50'
related:
  - "[[2026-08-01-agent-panel-plan]]"
---

# House main's unhoused degraded-intent in the begin idiom: the begin state must not invite a prompt when the data plane is degraded and starting would fail, using the parked common:agent.transcript.unavailable vocabulary

## Scope

- `frontend/src/app/agent/AgentBeginView.tsx`

## Description

- Add a begin-posture rule beside the composer posture: the begin state withholds its invitation (headline, starters, recents) when the served agent plane is unavailable, rendering the shared degraded state block with the parked unavailable vocabulary instead; the composer stays mounted per the status-never-blocks law.
- Derive the verdict from the SAME selector-state seam the Team selector's disabled reason reads, so the headline and the selector cannot disagree; an unsettled first read keeps the invitation - announcing a failure that has not happened is the louder lie.
- The served tier reason stays off the state block per the state-mode-uniformity law; it reaches the user on the selector's disabled title in the same view.

## Outcome

Landed in commit cb6d1779ac. Would-go-red proven by neutralizing the rule and watching both suites fail on exactly the withholding assertions; 34 targeted tests plus the nine localization gates green; all scanners clean. The parked vocabulary key needed no completion - three touch points were already correct and the step supplied the missing consumer.

## Notes

Houses the intent main's merge left unhoused after the begin idiom retired the transcript's no-session branch. The earlier degraded-path capture (inviting headline beside a disabled selector) is the exact defect this closes; re-running that capture under a degraded tier should now show the state block.
