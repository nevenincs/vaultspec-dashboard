---
tags:
  - '#exec'
  - '#agent-panel'
date: '2026-08-01'
modified: '2026-08-01'
body_schema: 'body-v1'
body_hash: 'sha256:3f4255afcd2faf702475043b721703a60482d4c5014ba401bebe729bad349b11'
step_id: 'S39'
related:
  - "[[2026-08-01-agent-panel-plan]]"
---

# Repair the dev-tooling scanner roots (localization, px, tokens, figma-names, module-size) and the justfile invocation path so every frontend gate command actually runs, grandfathering pre-existing module-size violators explicitly

## Scope

- `frontend/dev/tooling/`

## Description

- Fix one mistake repeated five times: the dev-tooling scanners moved a level deeper without their relative roots following (localization, px, module-size, figma-names, token-drift, plus the style-dictionary transform path) and the justfile rust lane still invoked the deleted path.
- Arm the module-size gate with a ratchet grandfather list: a new file at the ceiling fails, and a grandfathered file that grows past its recorded size fails - debt pinned, not forgiven.
- Pin the vitest worker bound in config so the suite does not depend on a remembered flag.

## Outcome

Landed in commit 18d4f6af55. All eight frontend gate commands pass AND were proven able to go red with planted violations. The armed size gate found one inherited violator (the a2a lifecycle route module at 1627, grandfathered as a ratchet); the armed localization scanner found five real violations in this campaign's own new modules, fixed in the same commit.

## Notes

Recorded plainly: P06 through P08 passed a localization gate that was not actually running - the vitest catalog tests were green but the source scanner was blind. Also caught: the builder's own bounded-suite wrapper echoed success while executing nothing. Together with the blind size scanner and the fixture-derived test bounds found earlier, that is three instances this campaign of checks that pass while proving nothing - the closeout should codify the lesson.
