---
tags:
  - '#exec'
  - '#agent-panel'
date: '2026-08-01'
modified: '2026-08-01'
body_schema: 'body-v1'
body_hash: 'sha256:1d6da0a8a0d159262c95d05a5627c4573fd08930e05b5c667539602c40eaf0a8'
step_id: 'S45'
related:
  - "[[2026-08-01-agent-panel-plan]]"
---

<!-- FRONTMATTER RULES:
     tags: one directory tag (hardcoded #exec) and one feature tag.
     Replace agent-panel with a kebab-case feature tag, e.g. #foo-bar.
     Additional tags may be appended below the required pair.

     modified: CLI-maintained last-modified stamp; set at scaffold time,
     refreshed by mutating CLI verbs and vault check fix; never hand-edit.

     step_id is the originating Step's canonical identifier, e.g. S01.
     The S45 and 2026-08-01-agent-panel-plan placeholders are machine-filled by
     `vaultspec-core vault add exec`; do not fill them by hand.

     Related: use wiki-links as '[[yyyy-mm-dd-foo-bar-plan]]' and link the
     parent plan.

     DO NOT add fields beyond those scaffolded; metadata lives
     only in the frontmatter. -->

<!-- LINK RULES:
     - [[wiki-links]] are ONLY for .vault/ documents in the related: field above.
     - NEVER use [[wiki-links]] or markdown links in the document body.
     - NEVER reference file paths in the body. If you must name a source file,
       class, or function, use inline backtick code: `src/module.py`. -->

<!-- STEP RECORD:
     This file represents one Step from the originating plan. Identified
     by its canonical leaf identifier (S##) and ancestor display path.
     The Collapse the autonomy control into a composer pill consistent with the row-of-pills grammar and the reference-verified C6 anatomy, fixing the row-2 overlap at split width and ## Scope

- `frontend/src/app/agent/Composer.tsx` placeholders below are machine-filled
     by `vaultspec-core vault add exec` from the originating Step row;
     do not fill them by hand. -->

# Collapse the autonomy control into a composer pill consistent with the row-of-pills grammar and the reference-verified C6 anatomy, fixing the row-2 overlap at split width

## Scope

- `frontend/src/app/agent/Composer.tsx`

## Description

- Collapse the autonomy control into a composer pill naming the served mode, with the two-option control behind it - the reference-verified C6 anatomy (both reference apps render approval mode as a compact pill beside the model selector), fixing the row-2 overlap the labelled block caused at split width.
- A served mode the toggle does not offer is named verbatim on the pill rather than rounded to a posture the worktree is not in; the expanded control is the same review-station AutonomyControl moved, not reimplemented, so refusal feedback and busy lockout stay single-sourced.
- The placement guard and the live mode round-trip moved with the shape; the S39 scanner-message one-liners rode the same batch.

## Outcome

Delivered in commit 3cbedc1971 - which carries a docs subject and an S44 trailer because the team lead's governance commit swept the builder's pre-staged S45 index entries (the git-commits-the-index hazard, chained add-and-commit in one command). Content verified complete and correct at HEAD: red-proved shape assertions, the live POST mode round-trip green through the extra click, 340 tests green, all scanners clean.

## Notes

Provenance mapping for future readers: the pill implementation, its render test, the moved guards, the four catalog touch points, and the scanner one-liners all live in 3cbedc1971 despite its S44 subject. The lead's process correction: staging inspection and commit now run as separate commands in the shared tree, gated on the inspection output.
