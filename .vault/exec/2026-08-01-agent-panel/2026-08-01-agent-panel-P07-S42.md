---
tags:
  - '#exec'
  - '#agent-panel'
date: '2026-08-01'
modified: '2026-08-01'
body_schema: 'body-v1'
body_hash: 'sha256:a9c08080319894ac8650221aecba351f7dc6f1215627a7bca1611f21a7d4c7af'
step_id: 'S42'
related:
  - "[[2026-08-01-agent-panel-plan]]"
---

# Consume the already-served run-status roles, frozen profile id, and per-role assignments in the team-run adapter and complete the run-header roster with per-role model (sources stay honestly absent - genuinely unserved)

## Scope

- `frontend/src/stores/server/agent/a2aTeam.ts`

## Description

- Seed the roster from authoritative run-status roles refreshed by relay frames (reload-safe), show per-role provider and model from the frozen profile assignments, and render real served elapsed that ticks while live and falls silent when nothing usable is served.

## Outcome

Landed in commit 85e5fc5c8f with a live-stack correction in 2d897cbfc7: the original adapters keyed on GUESSED field names (role/model) and dropped every served assignment and role - all fixtures were beliefs, not captures. The correction adapts the real wire (role_id/agent_id/model_name/provider_ready, options as plain strings) and pins it with a verbatim-capture fixture proven to go red under the old field names.

## Notes

CORRECTION to the earlier record: the claimed real served elapsed was FALSE - RunStatusResponse serves no start time (the assumed created_at belongs to the history record), and the header rightly fails closed and omits elapsed. Sources AND elapsed both stay honestly absent. The durable lesson joins the museum: fixtures must be captures of the wire, never guesses at it.
