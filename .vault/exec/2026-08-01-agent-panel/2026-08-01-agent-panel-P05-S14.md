---
tags:
  - '#exec'
  - '#agent-panel'
date: '2026-08-01'
modified: '2026-08-01'
body_schema: 'body-v1'
body_hash: 'sha256:3ecb57e9c9114ab1eb8a2f048ac95d72a751defac5bd20c35414dd3e43f0bd61'
step_id: 'S14'
related:
  - "[[2026-08-01-agent-panel-plan]]"
---

# Generalize graphVisible to the tri-state centerSlot (graph, agent, none) with migration of persisted layout state

## Scope

- `frontend/src/stores/view/shellLayout.ts`

## Description

- Generalize the boolean graph-visibility shell verb to the tri-state `centerSlot` (graph, agent, none) in the view store, surfaced as slot selectors and per-slot toggles.
- Migrate the legacy boolean at the read boundary (`normalizeCenterSlot`: true reads graph, false reads none) - no persisted blob rewrite, because no durable store of the old flag exists.

## Outcome

Landed in commit 6f64535e3a on feature/agent-panel. Boundary-normalizer test pins the legacy reading. Typecheck, eslint, prettier clean; targeted vitest across the six slot-adjacent store suites 89 passed.

## Notes

Lead-approved deviation: the step assumed a persisted layout blob carrying the old flag; none exists, so the migration is a read-boundary normalizer instead.
