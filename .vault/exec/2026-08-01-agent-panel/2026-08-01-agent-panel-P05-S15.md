---
tags:
  - '#exec'
  - '#agent-panel'
date: '2026-08-01'
modified: '2026-08-01'
body_schema: 'body-v1'
body_hash: 'sha256:5d0332481f9c7bf800b73af3aee6db282a34ea084355f364d6d07b9f3c9403df'
step_id: 'S15'
related:
  - "[[2026-08-01-agent-panel-plan]]"
---

# Add the reserved __agent__ panel reconciliation beside __graph__ and host the AgentPanel body in it

## Scope

- `frontend/src/app/stage/DockWorkspace.tsx`

## Description

- Reserve `__agent__` as a second dock panel beside `__graph__`, hosting the AgentPanel body as plain React (no portal - panel state lives in external stores).
- Extract the reconcile logic into a pure planner module `centerSlotPlan.ts` so slot exclusivity is unit-testable without a live dockview; the planner removes the incumbent before adding the wanted occupant and resolves a both-docked race to the wanted one.
- Move the agent lifecycle SSE subscription into a shell-mounted `AgentLifecycleHost`, keeping the run feed app-lifetime now that the panel unmounts whenever the graph holds the slot.

## Outcome

Landed in 6f64535e3a. Eleven planner unit cases green. Leaving the graph slot is asserted byte-identical to the shipped graph-hide removePanel path; the canvas host is an app-lifetime sibling of the dockview container and is never re-parented.

## Notes

Honesty limit recorded in the test header: at this layer the assertion proves removePanel-identity, not DOM-node identity, because the canvas is not a dockview panel at all.
