# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.6.0.schema.json
# The WinGet version manifest: the composite product identity + publisher.
# Authored here; PUBLISHED cross-repo to microsoft/winget-pkgs (a manifest PR)
# once a release attaches the composed product archive the installer manifest
# names. PackageVersion must match the installer and locale manifests exactly.
PackageIdentifier: vaultspec.vaultspec
PackageVersion: 0.1.10
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.6.0
