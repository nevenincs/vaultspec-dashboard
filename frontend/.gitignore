node_modules
dist
tsconfig.tsbuildinfo
.playwright-mcp

# Local live-verify scratch scripts + screenshots (never committed)
verify-*.cjs
verify-*.png

# vitest localStorage persistence artifact (per-machine, never committed)
.vitest-localstorage
