{
  "$schema": "https://design-tokens.github.io/community-group/format/",
  "$description": "Spacing scale (figma-parity-reconciliation ADR; research F1: spacing already MATCHES the Figma binding - 4-base 2,4,6,8,12,16,24,32). This source brings the existing scale under the generated non-color token pipeline so spacing parity is mechanical rather than hand-policed. The rem-px basis is 16px. Emits only the canonical --spacing-fg-* CSS custom properties through the pipeline.",
  "spacing": {
    "0-5": { "$description": "2px", "$type": "dimension", "$value": "0.125rem" },
    "1": {
      "$description": "4px (base step)",
      "$type": "dimension",
      "$value": "0.25rem"
    },
    "1-5": { "$description": "6px", "$type": "dimension", "$value": "0.375rem" },
    "2": { "$description": "8px", "$type": "dimension", "$value": "0.5rem" },
    "3": { "$description": "12px", "$type": "dimension", "$value": "0.75rem" },
    "4": { "$description": "16px", "$type": "dimension", "$value": "1rem" },
    "6": { "$description": "24px", "$type": "dimension", "$value": "1.5rem" },
    "8": { "$description": "32px", "$type": "dimension", "$value": "2rem" }
  }
}
